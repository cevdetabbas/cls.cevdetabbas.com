<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Tech App with Mr. Oz</title>

  <style>
    :root{
      /* Tesla-ish: clean, wide, modern. If “Tesla” font isn't available, fall back gracefully */
      --tesla-font: "Tesla", "Tesla Sans", "Gotham SSm", "Gotham", "Helvetica Neue",
                    Helvetica, Arial, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;

      --bg: #0b0b0c;
      --fg: #f5f5f7;
      --muted: rgba(245,245,247,0.85);
    }

    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: var(--tesla-font);
      background: #111;
      color: var(--fg);
    }

    /* Header */
    .site-header{
      background: var(--bg);
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }

    .header-inner{
      max-width: 1200px;
      margin: 0 auto;
      padding: 18px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
    }

    .brand{
      font-size: 44px;
      line-height: 1.05;
      letter-spacing: 0.2px;
      font-weight: 600;
      white-space: nowrap;
    }

    .datetime{
      text-align: right;
      white-space: nowrap;
      font-weight: 500;
      color: var(--muted);
    }

    .date-line{
      font-size: 22px; /* ~half of 44px */
      line-height: 1.1;
    }

    .time-line{
      font-size: 22px; /* same size for visibility */
      line-height: 1.1;
      margin-top: 4px;
      color: var(--fg);
    }

    /* Small screens */
    @media (max-width: 700px){
      .header-inner{
        flex-direction: column;
        align-items: flex-start;
      }
      .datetime{
        text-align: left;
      }
      .brand{
        font-size: 36px;
      }
      .date-line, .time-line{
        font-size: 18px;
      }
    }
  </style>
</head>

<body>
  <header class="site-header">
    <div class="header-inner">
      <div class="brand">Tech App with Mr. Oz</div>

      <div class="datetime" aria-label="Current date and time">
        <div class="date-line" id="dateLine">—</div>
        <div class="time-line" id="timeLine">—</div>
      </div>
    </div>
  </header>

  <script>
    function updateDateTime() {
      const now = new Date();

      const dateText = now.toLocaleDateString(undefined, {
        weekday: "long",
        year: "numeric",
        month: "long",
        day: "numeric"
      });

      const timeText = now.toLocaleTimeString(undefined, {
        hour: "2-digit",
        minute: "2-digit",
        second: "2-digit"
      });

      document.getElementById("dateLine").textContent = dateText;
      document.getElementById("timeLine").textContent = timeText;
    }

    updateDateTime();
    setInterval(updateDateTime, 1000);
  </script>
</body>
</html>

