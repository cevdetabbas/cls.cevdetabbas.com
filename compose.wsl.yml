services:
  web:
    image: httpd:2.4
    container_name: cls.cevdetabbas.com
    ports:
      - "${WSL_IP:?Set WSL_IP or run ./start-cls-wsl.sh}:2880:80"
    volumes:
      - ./htdocs:/usr/local/apache2/htdocs:ro
    restart: unless-stopped
